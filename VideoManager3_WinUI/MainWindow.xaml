<Window
    x:Class="VideoManager3_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:local="using:VideoManager3_WinUI"
    xmlns:xaml="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="TagItemTemplate" x:DataType="xaml:TreeViewNode">
                <StackPanel Orientation="Horizontal" Height="24">
                    <Rectangle Width="16" Height="16" Fill="{x:Bind ((local:TagItem)Content).Color}" Margin="0,0,8,0"/>
                    <TextBlock Text="{x:Bind ((local:TagItem)Content).Name}" VerticalAlignment="Center"/>
                </StackPanel>
            </DataTemplate>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*" MinWidth="150"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="700*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- 
        【修正点】
        - ItemsSourceプロパティを削除
        - TreeViewに x:Name="TagsTreeView" を追加して、コードからアクセスできるようにする
        -->
        <TreeView x:Name="TagsTreeView"
                  Grid.Column="0"
                  ItemTemplate="{StaticResource TagItemTemplate}"
                  Margin="5"/>

        <controls:GridSplitter Grid.Column="1"
                               Width="12"
                               ResizeBehavior="BasedOnAlignment"
                               ResizeDirection="Auto"
                               Background="Transparent"
                               HorizontalAlignment="Center" />

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="7*" MinHeight="200"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="3*" MinHeight="100"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#e0e0e0" Margin="5">
                <TextBlock Text="右ペイン (ファイル一覧)" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <controls:GridSplitter Grid.Row="1"
                                   Height="12"
                                   ResizeBehavior="BasedOnAlignment"
                                   ResizeDirection="Auto"
                                   Background="Transparent"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Stretch"/>

            <Border Grid.Row="2" Background="#d0d0d0" Margin="5">
                <TextBlock Text="下部ペイン (詳細情報)" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</Window>