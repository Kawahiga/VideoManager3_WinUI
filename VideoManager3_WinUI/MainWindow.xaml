<Window
    x:Class="VideoManager3_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideoManager3_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 上部コマンドバー -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="フォルダの追加" Command="{x:Bind ViewModel.AddFolderCommand}"/>
            <AppBarButton Icon="Tag" Label="タグの追加" Command="{x:Bind ViewModel.AddTagCommand}"/>
        </CommandBar>

        <!-- メインコンテンツエリア -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!-- 左ペイン: タグツリー -->
                <ColumnDefinition Width="200" MinWidth="100"/>
                <!-- 縦方向のGridSplitter -->
                <ColumnDefinition Width="5"/>
                <!-- 右エリア全体 -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左ペイン: タグツリー -->
            <TreeView Grid.Column="0"
                      ItemsSource="{x:Bind ViewModel.TagItems}"
                      SelectedItem="{x:Bind ViewModel.SelectedTagItem, Mode=TwoWay}">
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="local:TagItem">
                        <TreeViewItem ItemsSource="{x:Bind Children}" Content="{x:Bind Name}"/>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- 縦方向のGridSplitter -->
            <controls:GridSplitter Grid.Column="1" Width="5" ResizeBehavior="BasedOnAlignment" ResizeDirection="Columns" Background="LightGray"/>

            <!-- 右エリア: ここに上下分割のGridを配置 -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <!-- 右上ペイン: ファイルリスト -->
                    <RowDefinition Height="*" MinHeight="150"/>
                    <!-- 横方向のGridSplitter -->
                    <RowDefinition Height="5"/>
                    <!-- 右下ペイン: 詳細情報 -->
                    <RowDefinition Height="250" MinHeight="150"/>
                </Grid.RowDefinitions>

                <!-- 右上ペイン: ファイルリスト -->
                <ListView Grid.Row="0"
                          ItemsSource="{x:Bind ViewModel.VideoItems}"
                          SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:VideoItem">
                            <TextBlock Text="{x:Bind Name}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- 横方向のGridSplitter -->
                <controls:GridSplitter Grid.Row="1" Height="5" ResizeBehavior="BasedOnAlignment" ResizeDirection="Rows" Background="LightGray"/>

                <!-- 右下ペイン: 詳細情報 -->
                <Grid Grid.Row="2" Padding="10">
                    <StackPanel Visibility="{x:Bind ConvertObjectToVisibility(ViewModel.SelectedItem), Mode=OneWay}">
                        <TextBlock Text="{x:Bind GetItemName(ViewModel.SelectedItem), Mode=OneWay}" FontWeight="Bold" TextWrapping="Wrap"/>
                        <TextBlock Text="{x:Bind FormatLastModified(ViewModel.SelectedItem), Mode=OneWay}" Margin="0,5,0,0"/>
                        <TextBlock Text="{x:Bind GetItemPath(ViewModel.SelectedItem), Mode=OneWay}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>

