<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FileMover.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileMover"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FileMover">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <ItemsControl ItemsSource="{Binding FileMoveTasks}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="ファイルを移動しています..." FontWeight="Bold"/>
                        <TextBlock Text="{Binding SourceAndDestinationText}" Foreground="Gray"/>
                        <ProgressBar Value="{Binding ProgressPercentage}" Maximum="100"/>
                        <TextBlock Text="{Binding ProgressText}" HorizontalAlignment="Left"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
